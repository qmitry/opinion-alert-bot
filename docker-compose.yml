version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_DB: opinion_alerts
      POSTGRES_USER: botuser
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U botuser"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - bot-network

  bot:
    build: .
    restart: always
    environment:
      OPINION_API_KEY: ${OPINION_API_KEY}
      TELEGRAM_TOKEN: ${TELEGRAM_TOKEN}
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: opinion_alerts
      DB_USER: botuser
      DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - bot-network

volumes:
  postgres_data:

networks:
  bot-network:
    driver: bridge